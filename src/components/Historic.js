import Header from './Header';
import Menu from './Menu';

export default function History() {
    return (
        <main>
            <Header></Header>
            <Menu></Menu>
        </main>
    );
}
